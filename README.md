# hello-csharp

A small interactive C# console app for learning core language features.

Run it with:

```bash
dotnet run
```

The app asks a few questions, builds a typed `User`, prints facts with LINQ-powered comparisons, makes a few friendly jokes, then exits.
